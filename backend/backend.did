service : {
  addTranslation: (text, text) -> ();
  getTranslations: () -> (vec record {
                                text;
                                text;
                              }) query;
}
